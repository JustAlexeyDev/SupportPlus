# 🔧 Настройка переменных окружения (.env)

Проект использует `.env` файлы для конфигурации серверов. Это позволяет легко настраивать приложение без изменения кода.

## Быстрая настройка

Запустите скрипт для автоматического создания .env файлов:

```bash
./setup-env.sh
```

Или создайте файлы вручную, скопировав примеры:

```bash
cp Backend/.env.example Backend/.env
cp Frontend/.env.example Frontend/.env
```

## Backend (.env)

Файл: `Backend/.env`

### Основные переменные

```env
# Режим работы
NODE_ENV=development  # development | production

# Сервер
PORT=8000             # Порт бэкенда
HOST=0.0.0.0          # Хост (0.0.0.0 для доступа из сети)

# Frontend URL (для OAuth редиректов)
FRONTEND_URL=http://localhost:3000

# База данных
DB_TYPE=sqlite        # Тип БД
DB_DATABASE=supportplus.db
DB_SYNCHRONIZE=true   # Автосинхронизация схемы
DB_LOGGING=true       # Логирование SQL

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (опционально, в dev режиме разрешены все локальные IP)
# CORS_ORIGINS=http://localhost:3000,http://192.168.1.100:3000

# Swagger
SWAGGER_PATH=api
SWAGGER_TITLE=SupportPlus API
```

### Для доступа с телефона

Если хотите указать конкретный IP для CORS:

```env
CORS_ORIGINS=http://192.168.43.204:3000,http://localhost:3000
FRONTEND_URL=http://192.168.43.204:3000
```

## Frontend (.env)

Файл: `Frontend/.env`

### Основные переменные

```env
# API URL (оставьте пустым для автоопределения)
REACT_APP_API_URL=

# Или укажите явно:
# REACT_APP_API_URL=http://192.168.43.204:8000
```

### Автоопределение API URL

Если `REACT_APP_API_URL` не указан, приложение автоматически определяет URL API:
- Если открыто с `localhost` → API будет `http://localhost:8000`
- Если открыто с IP (например, `192.168.43.204:3000`) → API будет `http://192.168.43.204:8000`

### Переменные для dev сервера

React-scripts автоматически использует переменные окружения системы:
- `HOST` - хост для dev сервера (по умолчанию 0.0.0.0)
- `PORT` - порт для dev сервера (по умолчанию 3000)

Можно задать в .env или при запуске:

```bash
# В .env (не работает напрямую, нужно через скрипт)
# Или при запуске:
HOST=0.0.0.0 PORT=3000 npm run dev
```

## Использование

### Backend

Все переменные загружаются автоматически через `@nestjs/config`:

```typescript
// В коде используется ConfigService
const port = configService.get<number>('PORT') || 8000;
```

### Frontend

React-scripts автоматически загружает переменные с префиксом `REACT_APP_`:

```javascript
// В коде доступны через process.env
const apiUrl = process.env.REACT_APP_API_URL;
```

## Приоритет загрузки

### Backend
1. `.env.local` (если существует)
2. `.env`
3. Системные переменные окружения

### Frontend
1. `.env.development.local` (в dev режиме)
2. `.env.local`
3. `.env.development` (в dev режиме)
4. `.env`
5. Системные переменные окружения

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файлы в git
- Используйте `.env.example` как шаблон
- В production используйте переменные окружения системы или секреты

## Примеры конфигураций

### Локальная разработка (только компьютер)

**Backend/.env:**
```env
NODE_ENV=development
PORT=8000
HOST=localhost
FRONTEND_URL=http://localhost:3000
```

**Frontend/.env:**
```env
REACT_APP_API_URL=http://localhost:8000
```

### Локальная разработка (с доступом с телефона)

**Backend/.env:**
```env
NODE_ENV=development
PORT=8000
HOST=0.0.0.0
FRONTEND_URL=http://192.168.43.204:3000
CORS_ORIGINS=http://192.168.43.204:3000,http://localhost:3000
```

**Frontend/.env:**
```env
REACT_APP_API_URL=http://192.168.43.204:8000
```

### Production

**Backend/.env:**
```env
NODE_ENV=production
PORT=8000
HOST=0.0.0.0
FRONTEND_URL=https://yourdomain.com
CORS_ORIGINS=https://yourdomain.com
JWT_SECRET=strong-random-secret-key
DB_SYNCHRONIZE=false
```

## Проверка конфигурации

После настройки .env файлов, проверьте что переменные загружаются:

**Backend:**
```bash
cd Backend
npm run start:dev
# В логах должны быть видны значения из .env
```

**Frontend:**
```bash
cd Frontend
npm run dev
# Откройте консоль браузера и проверьте process.env
```





